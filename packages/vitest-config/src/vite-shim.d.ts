// shim removed — relying on real `vite` package types from the workspace catalog.
// The file is preserved as a placeholder in case we need to reintroduce a shim,
// but it contains no types now so the TypeScript compiler will use the
// installed `vite` package's types instead.
