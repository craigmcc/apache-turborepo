/**
 * Utility functions for creating Prisma models from QBO API objects.
 */

// External Modules ----------------------------------------------------------

import {
  QboAccount
} from "@repo/qbo-api/types/Finance";

// Internal Modules ----------------------------------------------------------

// Public Objects ------------------------------------------------------------

// TODO: Replace Object with Account when Prisma models are defined
export function createAccount(qboAccount: QboAccount): Object {
  return {};
}
